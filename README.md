# homebrew-forktrust

Homebrew tap for [forktrust](https://github.com/binkovsky/forktrust) — safe-by-default git worktree manager for parallel AI coding sessions.

## Install

```bash
brew tap binkovsky/forktrust
brew install forktrust
```

Then:

```bash
forktrust --help
```
